user:
  valid_email_regex: !ruby/regexp /\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i
  email:
    min_length: 5
    max_length: 50
  name:
    max_length: 50
  password:
    min_length: 6
  phone_number:
    length: 10
  model:
    limitation: 6
